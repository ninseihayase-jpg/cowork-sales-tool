.venv/
__pycache__/
*.pyc
.env
service_account.json
*.local.json
.DS_Store

# 実データ（クライアント名・金額等の機微情報）はコミットしない
seed/
*.xlsx

# 週次レポート本文（社外秘）はGitに置かず、本番DB(weekly_reports)にのみ格納する。
# ローカルの下書きHTMLの置き場としてのみ使い、コミットはしない。
reports/


# 営業情報DB（ローカルデータ）
*.db
